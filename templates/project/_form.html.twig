<div class="cont-1">
        <div class="form-container project-form">
            <div class="content-form">
                <div class="container">
                    {{ form_start(form) }}

                        <fieldset class="form-group row form-item">
                            <div class="col-lg-12 label-form">
                               {{ form_row(form.title)}}
                            </div>
                        </fieldset>
                        <fieldset class="form-group row form-item label-form">
                            <div class="col-lg-12">
                                {{ form_row(form.summary)}}
                            </div>
                        </fieldset>
                        <fieldset class="form-group row form-item label-form">
                            <div class="col-lg-12">
                                 {{ form_row(form.description)}}
                            </div>
                        </fieldset>

                         <fieldset class="form-group row form-item label-form">
                            <div class="col-sm-8">
                                 {{ form_row(form.estimatio)}}
                            </div>
                        </fieldset>

                        <fieldset class="form-group row list-item-check form-item label-form">
                                {{ form_row(form.categories)}}
                        </fieldset>

                        <fieldset class="form-group row list-item-check form-item label-form">
                            {{ form_row(form.technologies)}}
                        </fieldset>


                        <div class="form-group login-button">
                            <div>
                                <button type="submit" class="btn btn-primary form-button">Ajouter Projet</button>
                            </div>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
            <div class="footer-form project-footer-form">
                Tu possedes dejà un compte ? <a href="">Connecte toi</a>
            </div>
        </div>
    </div>